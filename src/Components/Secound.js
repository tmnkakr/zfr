import React from "react";

export default function Secound(props) {
  let tag1 = null;
  // for (; props.start > 0; props.start /= 10) {
  //   tag1 += <div>{props.start}</div>;
  // }
  return <div></div>;
}
